# Pacific delayed overview: recolored and smoothed single perturbation column

This is the final single-column variant requested after the TOS/ZOS pseudo-3D trial:

- the left column remains one initial-delta-TOS map per case;
- its colors are remapped to the exact overview TOS/SSTA palette used by the right three columns;
- masked Gaussian smoothing and 3× cubic display refinement are applied only to the left map interiors;
- truth, baseline, perturbed columns, labels, layout, and colorbars remain unchanged.

No CNOP or forecast values were recomputed. A copy is also in `C:\Users\zhen.luo\Desktop\article\pacific_delay_overview_recolored_smoothed.png`.
